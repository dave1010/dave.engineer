---
layout: base.njk
---
<main class="px-6 py-16 sm:px-10">
  <article class="mx-auto flex max-w-3xl flex-col gap-8">
    {% if title or sectionLabel or description %}
      <header class="flex flex-col gap-4 text-stone-100">
        {% if sectionLabel %}
          <p class="text-sm text-orange-300">{{ sectionLabel }}</p>
        {% endif %}
        {% if title %}
          <h1 class="font-serif text-3xl font-semibold text-stone-100 sm:text-4xl">{{ title }}</h1>
        {% endif %}
        {% if description %}
          <p class="text-lg text-stone-400">{{ description }}</p>
        {% endif %}
      </header>
    {% endif %}
    <div class="prose prose-invert prose-lg max-w-none">
      {{ content | safe }}
    </div>
  </article>
</main>
